Identificador de fluxo: JID
Porta: aleatória

=> Possíveis comandos do client para o servidor:

INI(length, host, display) - Inicializa o servidor
->RET(length, host) - mensagem contendo host do servidor

QUT(length) - Comando para que o servidor se desligue

LST(length=0) - lista os jobs (múltiplas mensagens JOB)
*>JOB(length, JID, status, título, data de início, data fim, host que executou, erros ou avisos, linha de comando, saida)

RUN(length, flow) - executa um fluxo
->RET(length, JID, status, título, data de ínicio, erros ou avisos, linha de comando, saida)

FLW(length, JID) - pede fluxo
->RET(length, flow)

KIL(length, JID) - mata um job (sinal FIN emitido quando terminado)
END(length, JID) - finaliza um job com educação (sinal FIN emitido quando terminado)

CLR(length, JID) - remove processo JID (caso não esteja sendo executado) da lista de processos

=> Sinais do servidor para o cliente:

OUT(length, JID, nova saida) - o job JID emitiu saída
FIN(length, JID) - Informa o término de um processo

JOB(definição acima) - emitido para todos os clientes que um novo job foi iniciado
